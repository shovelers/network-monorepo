<!DOCTYPE html>
<html lang="en">
  <head>
  </head>
  <body>
    <script type="module">
      import { dial, writeData, readFile, CID, writePrivateData, readPrivateFile, createExchangeRoot, acceptShare, createBrowserNode, ExamplePublicFile} from '/helia_pds_browser.js';
      window.createBrowserNode = createBrowserNode;
      window.ExamplePublicFile = ExamplePublicFile;
      window.dial = dial;
      window.writeData = writeData;
      window.readFile = readFile;
      window.CID = CID;
      window.writePrivateData = writePrivateData;
      window.readPrivateFile = readPrivateFile;
      window.createExchangeRoot = createExchangeRoot;
      window.acceptShare = acceptShare;
    </script>
    <div class="flex flex-col justify-evenly items-center h-screen bg-neutral">
      <div class="text-5xl font-bold tracking-tight underline underline-offset-4 decoration-4 decoration-accent">Helia PDS new</div>
    </div>
  </body>
</html>