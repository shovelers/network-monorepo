{
  "name": "rolodex",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "rollup -cm rollup.config.js && npx tailwindcss build -i ./public/styles/tailwind.css -o ./public/styles/style.css",
    "build-dev": "rollup -cm rollup.config.js && npx tailwindcss build -i ./public/styles/tailwind.css -o ./public/styles/style.css --watch",
    "start": "npm run build && node index.js",
    "dev": "npm run build-dev && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@oddjs/odd": "^0.37.2",
    "ejs": "^3.1.8",
    "express": "^4.18.2",
    "rollup": "^3.29.1",
    "@rollup/plugin-node-resolve": "^15.2.1",
    "daisyui": "^3.7.4",
    "tailwindcss": "^3.3.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
